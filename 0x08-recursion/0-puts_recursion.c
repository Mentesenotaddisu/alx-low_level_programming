#include "main.h"
/**
 *_puts_recursion - entry
 *@s: string
 * main - entry
 *Return: 0
 */
void _puts_recursion(char *s)
{
	_putchar("%s\n", _puts_recursion(char *s);
			return;
}
int main(void)
{
	_puts_recursion();
	return (0);
}
