#include "main.h"

/**
 * _strcat - entry
 * @dest: char 1
 * @str:char 2
 * Return: Always 0
 */

char *_strcat(char *dest, char *src)
{
	int x;
	int y;

	x = 0;
	while (dest[x] != '\0';)
			{
		x++;
		}
		y = 0;
	while (str[y] != '\0';)
		{
		x++;
		y++;
		}
	dest[x] = '\0';
		return (dest);
}

