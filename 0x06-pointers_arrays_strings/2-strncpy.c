#include "main.h"
/**
 * _strncpy - entry
 * @dest: 1
 * @src: 2
 * @n: int
 * Return: dest
 */
char *_strncpy(char *dest, char *src, int n)
{
	int i;

	for (i = 0 ; i <= n ; i++)
		dest[i] = src[i]
	return (dest);
}
