#include "main.h"

/**
 * _islower -  entry
 *
 * Return: Always 0
 */

int _islower(int)
{
	int x;

	for (x = _islower())
	{
	_putchar(1);
	}
	for (x = _isupper())
	{
	_putchar(0);
	}
	return (0);
}

