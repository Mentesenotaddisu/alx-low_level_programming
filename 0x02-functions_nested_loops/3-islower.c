#include "main.h"

/**
 * _islower -  entry
 *
 * Return: Always 0
 */

int _islower(int )
{
	int x;

	for (x != _islower)
	{
	_putchar(0);
	}
	for (x != _isupper)
	{
	_putchar(1);
	}
	return (0);
}

