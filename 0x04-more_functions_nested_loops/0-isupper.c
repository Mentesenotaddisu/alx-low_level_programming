#include "main.h"
#include <stdio.h>
/**
 * _isupper - entry
 *
 * @x: check
 * Return: 1 for upper or 0 for else
 */

int _isupper(int c)
{

	if (c >= 65 && c <= 98)
	{
	return (1);
	}
	return (0);
}
